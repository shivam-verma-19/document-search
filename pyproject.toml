[tool.isort]
profile = "black"
line_length = 88

[tool.coverage.run]
source = ["backend"]
omit = [
    "*/tests/*",
    "*/conftest.py",
    "*/stubs/*",
    "*/mocks/*"
]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "if TYPE_CHECKING:",
    "raise NotImplementedError"
]